pipeline {
    
    agent { label 'main' }
    stages {
        stage('commit') {
            steps {
                git 'https://github.com/Indhuja-R/task5.git'
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
}
